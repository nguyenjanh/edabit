public class Challenge {
	public static String sevenBoom(int[] arr) {
		
		for (int a = 0; a < arr.length; a++) {
			
			String current = "" + arr[a];
			
			if (current.indexOf("7") > -1)
				return "Boom!";
		}
		
		return "there is no 7 in the array";
		
	}
}
